# 🎰 Miracless Fart - Telegram Lottery Bot

## 📋 Описание проекта
Современное Telegram Web App для проведения лотерей с системой администраторов и транзакций.

### **🎯 Важное обновление: PostgreSQL вместо Firebase**
✅ **Код полностью переписан** для использования PostgreSQL через Prisma
✅ **Firebase удален** - теперь используется только PostgreSQL
✅ **Надежное хранение** - данные не теряются при перезапуске
✅ **Автоматические бэкапы** - встроенная защита данных
✅ **Масштабируемость** - до миллионов пользователей

## 🏗️ Архитектура

### **База данных: PostgreSQL + Prisma (Продакшен)**
- ✅ **Надежное хранение** - данные не теряются при перезапуске
- ✅ **Автоматические бэкапы** - встроенная защита данных
- ✅ **ACID транзакции** - гарантированная целостность
- ✅ **TypeScript поддержка** - типизированные запросы
- ✅ **Масштабируемость** - до миллионов пользователей

### **База данных: SQLite + Prisma (Разработка)**
- ✅ **Бесплатно** - встроено в Node.js
- ✅ **Легкая настройка** - 5 минут
- ✅ **Локальная разработка** - работает без интернета
- ✅ **Быстрое тестирование** - мгновенные операции

### **Фронтенд: React**
- Telegram Web App интеграция
- Адаптивный дизайн
- Real-time обновления

### **Бэкенд: Node.js + Express**
- RESTful API
- JWT аутентификация
- Двухуровневая система администраторов

## 👥 Система администраторов

| ID | Роль | Права |
|----|------|-------|
| `5206288199` | **Главный админ** | Полный доступ ко всем функциям |
| `1329896342` | **Ограниченный админ** | Только просмотр и базовые операции |

## 🚀 Быстрый запуск

### **Локальная разработка (SQLite):**
```bash
# Backend
cd backend
npm install
npx prisma generate
npx prisma db push  # Создает SQLite файл dev.db
npm start

# Frontend (в новом терминале)
cd frontend
npm install
npm start
```

### **Продакшен (PostgreSQL):**
```bash
# 1. Создайте PostgreSQL в Render.com
# 2. Добавьте DATABASE_URL в переменные окружения
# 3. Разверните через Git
git push origin main
```

## 🐘 Настройка PostgreSQL в Render

### **Шаг 1: Создание базы данных**
1. Перейдите на [render.com](https://render.com)
2. Нажмите **"New"** → **"PostgreSQL"**
3. Выберите план (Starter - $7/месяц)
4. Укажите имя базы данных
5. Нажмите **"Create Database"**

### **Шаг 2: Получение DATABASE_URL**
1. В панели управления базой данных
2. Перейдите во вкладку **"Info"**
3. Скопируйте **"External Database URL"**
4. Это будет ваша `DATABASE_URL`

### **Шаг 3: Настройка переменных окружения**
В Render dashboard вашего веб-сервиса:
1. **Environment** → **Environment**
2. Добавьте переменную:
   ```
   DATABASE_URL=postgresql://username:password@hostname:5432/database_name
   ```
3. Добавьте остальные переменные из `.env.example`

### **Шаг 4: Первая миграция**
```bash
# После развертывания, подключитесь к серверу
npx prisma db push
```

### **Шаг 5: Проверка работы**
```bash
# Проверьте логи Render
# Должно быть: "Database schema updated successfully"
```

## 🔍 **Как смотреть базу данных в Render**

### **Способ 1: Prisma Studio (рекомендуется)**
```bash
# 1. Установите локально PostgreSQL клиент
npm install -g prisma

# 2. Создайте локальный .env файл с DATABASE_URL из Render
echo "DATABASE_URL=postgresql://username:password@hostname:5432/database_name" > .env

# 3. Запустите Prisma Studio
cd backend
npx prisma studio --port 5555

# 4. Откройте http://localhost:5555 в браузере
```

### **Способ 2: Подключение через терминал**
```bash
# Используйте psql клиент
psql "postgresql://username:password@hostname:5432/database_name"

# Или через pgAdmin
# 1. Скачайте pgAdmin
# 2. Создайте новое подключение
# 3. Введите данные из DATABASE_URL
```

### **Способ 3: Через Render Dashboard**
```bash
# 1. Зайдите в Render.com
# 2. Выберите вашу PostgreSQL базу данных
# 3. Перейдите в раздел "Connections"
# 4. Используйте встроенный браузер БД
```

### **Способ 4: Готовый скрипт (рекомендуется)**
```bash
# 1. Скопируйте пример файла
cd backend
cp .env.render.example .env

# 2. Отредактируйте .env файл
# Замените DATABASE_URL на реальный из Render
nano .env

# 3. Запустите просмотр базы данных
node view-db.js
```

**Пример .env файла для Render:**
```bash
# Скопируйте из backend/.env.render.example
DATABASE_URL="postgresql://miracless_user:your_password@hostname:5432/miracless_db"
```

**Скрипт покажет:**
- 📊 Общую статистику (количество пользователей, лотерей, транзакций)
- 👥 Последних 5 пользователей с деталями
- 🎰 Активные лотереи
- 💰 Последние 5 транзакций
- 📝 Логи администратора

### **Пример вывода скрипта:**
```
🔍 Просмотр базы данных...

✅ Подключение к базе данных успешно!

📊 ОБЩАЯ СТАТИСТИКА:
👥 Пользователей: 150
🎰 Лотерей: 25
💰 Транзакций: 450
📝 Логов администратора: 89

👥 ПОСЛЕДНИЕ 5 ПОЛЬЗОВАТЕЛЕЙ:
1. Иван Петров (@ivan_petrov)
   ID: 123456789, Баланс: $50.00, Premium: ✅
   Создан: 31.08.2025, 19:30:15
```

### **Развертывание в Render:**
```bash
# Автоматическое развертывание через Git
git push origin main
```

## 📊 Сравнение баз данных

| Критерий | PostgreSQL + Prisma | SQLite + Prisma | Firebase |
|----------|---------------------|-----------------|----------|
| **Стоимость** | $0-15/месяц | Бесплатно | $0.026/GB/месяц |
| **Настройка** | 10 мин | 5 мин | 15-30 мин |
| **Надежность** | ✅ Максимальная | ⚠️ Эфемерная | ✅ Высокая |
| **Бэкапы** | ✅ Автоматические | ❌ Ручные | ✅ Автоматические |
| **Масштаб** | 1M+ пользователей | 1000+ пользователей | 100k+ пользователей |
| **Локальная разработка** | ❌ Сложная | ✅ Простая | ❌ Сложная |
| **Без интернета** | ❌ Требует сервер | ✅ Работает | ❌ Не работает |
| **Продакшен** | ✅ Идеально | ❌ Не рекомендуется | ✅ Хорошо |

## 🔧 Технические детали

### **База данных:**
- **PostgreSQL** - реляционная БД для продакшена
- **SQLite** - файловая БД для разработки
- **Prisma** - ORM с типизацией
- **Миграции** - автоматические обновления схемы

### **Настройка PostgreSQL:**
```bash
# 1. Создайте базу данных в Render.com
# 2. Скопируйте DATABASE_URL
# 3. Добавьте в переменные окружения

# Пример DATABASE_URL:
DATABASE_URL="postgresql://username:password@hostname:5432/database_name"
```

### **Безопасность:**
- Двухуровневая аутентификация
- Защита от SQL-инъекций
- Валидация данных

### **Производительность:**
- Оптимизированные запросы
- Кэширование данных
- Асинхронная обработка

## 📁 Структура проекта

```
miracless-fart/
├── backend/
│   ├── prisma/
│   │   └── schema.prisma    # Схема БД
│   ├── src/
│   │   ├── prisma.js        # Клиент Prisma
│   │   ├── index.js         # Главный сервер
│   │   └── auth.js          # Аутентификация
│   └── api/                 # API endpoints
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   └── services/
│   └── public/
└── package.json             # Скрипты сборки
```

## 🎯 Преимущества новой архитектуры

### **Для разработчика:**
- ⚡ Быстрый запуск проекта
- 🛠️ Простая отладка
- 📊 Легкое тестирование
- 🔄 Простые миграции

### **Для бизнеса:**
- 💰 Бесплатно для старта
- 📈 Легко масштабировать
- 🔒 Надежная работа
- 🚀 Быстрое развертывание

### **Для пользователей:**
- 📱 Работает на всех устройствах
- ⚡ Быстрая загрузка
- 🔐 Безопасные транзакции
- 🎮 Удобный интерфейс

## 🔄 Миграция с Firebase

Если нужно перейти с Firebase:

1. **Экспорт данных** из Firestore
2. **Создание схемы** в Prisma
3. **Импорт данных** в SQLite
4. **Обновление кода** для использования Prisma
5. **Тестирование** всех функций

## 🔍 **Отслеживание логов и отладка**

### **Где смотреть логи:**

#### **1. Логи Render (бэкенд)**
```bash
# В Render Dashboard → ваш Web Service → "Logs"
# Выберите "Live Logs" для реального времени
```

#### **2. Логи браузера (фронтенд)**
```javascript
// Откройте DevTools (F12) → Console
// Ищите сообщения с префиксами:
// 🚀 [AUTO-REG] - авторегистрация
// 🔐 [AUTH] - аутентификация
// 🌐 [API] - API запросы
// 🗄️ [SERVER] - запуск сервера
```

#### **3. Тестирование API**
```bash
# Запустите тест всех endpoints
cd backend
node test-api.js
```

#### **4. Диагностика подключения**
```bash
# Быстрая диагностика проблем с БД
npm run diagnose
# или
cd backend && node diagnose.js
```

### **Что показывают логи:**

#### **Успешная регистрация:**
```
🚀 [AUTO-REG] НАЧАЛО РЕГИСТРАЦИИ В БЭКЕНДЕ
📤 [AUTO-REG] Отправляемые данные: {...}
🌐 [AUTO-REG] Отправка POST запроса на /api/auth...
📊 [AUTO-REG] Статус ответа: 200
✅ [AUTO-REG] УСПЕШНЫЙ ПАРСИНГ JSON: {...}
🎉 [AUTO-REG] РЕГИСТРАЦИЯ ПРОШЛА УСПЕШНО
```

#### **Ошибка "Server returned empty response":**
```
❌ [AUTO-REG] ПУСТОЙ ОТВЕТ ОТ СЕРВЕРА
🔍 [AUTO-REG] Возможные причины:
   - Бэкенд не запущен
   - База данных не подключена
   - Ошибка в коде бэкенда
```

### **Возможные проблемы и решения:**

#### **Проблема: "Server returned empty response"**
**Решения:**
1. ✅ Запустите диагностику: `npm run diagnose`
2. ✅ Тестируйте PostgreSQL: `npm run test-postgres`
3. ✅ Проверьте статус бэкенда в Render
4. ✅ Посмотрите логи в Render Dashboard
5. ✅ Выполните `npx prisma db push` в терминале Render
6. ✅ Проверьте переменные окружения (DATABASE_URL)

#### **Проблема: "PERMISSION_DENIED"**
**Решения:**
1. ✅ Проверьте Firebase Service Account
2. ✅ Убедитесь, что переменная `FIREBASE_SERVICE_ACCOUNT` настроена
3. ✅ Проверьте права доступа к Firestore

#### **Проблема: "UNAVAILABLE"**
**Решения:**
1. ✅ Проверьте подключение к интернету
2. ✅ Firebase может быть временно недоступен
3. ✅ Попробуйте перезапустить бэкенд

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера (инструкции выше)
2. Убедитесь в наличии всех зависимостей
3. Проверьте подключение к БД
4. Используйте скрипт `test-api.js` для диагностики

---

**🎰 Удачи в разработке!** 🚀